<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="button_validate_disabled.PaymentScreenValidate" t-inherit="point_of_sale.PaymentScreenValidate" t-inherit-mode="extension">
        <xpath expr="//button[hasclass('btn-switchpane')]" position="replace">
            <button class="btn-switchpane validation-button btn btn-primary flex-fill py-4 rounded-0 fw-bolder fs-1"
               t-att-class="( !(currentOrder.is_paid() and currentOrder._isValidEmptyOrder()) ? ' secondary' : '' ) 
                + ( this.isProcessing.value ? ' disabled' : '' )"
                t-on-click="() => this.validateOrder()">
                <span>Validar</span>
            </button>
        </xpath>
        <xpath expr="//div[hasclass('button') and hasclass('next')]" position="replace">
            <div class="button next validation btn btn-primary btn-lg py-5 rounded-0 d-flex flex-column align-items-center justify-content-center fw-bolder" 
                t-attf-class="{{ (currentOrder.is_paid() and currentOrder._isValidEmptyOrder()) ? 'highlight' : '' }} {{ this.isProcessing.value ? 'disabled' : '' }}"
                t-on-click="() => this.validateOrder()"
                >
                <div class="pay-circle d-flex align-items-center justify-content-center mb-2">
                    <i class="oi oi-chevron-right" role="img" aria-label="Pay" title="Pay" />
                </div>
                <span class="next_text">Validar</span>
            </div>
        </xpath>
    </t>
</templates>
